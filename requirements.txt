# Core recognition engine (always required) -- modality-agnostic, reused as-is
torch>=2.2
torchvision>=0.17
numpy>=1.24
scipy>=1.10
pillow>=10.0

# ---------------------------------------------------------------------------
# Real-data pipeline (scalpel/data) -- v2 REPLACES the synthetic renderer.
# (open3d / BodyParts3D / Z-Anatomy meshes are gone; data comes from real
#  BlueLink QuizLink dissection PDFs.)
# ---------------------------------------------------------------------------
# crawl: QuizLink fetch -- the index renders Drive links via JS, so a headless
# browser is REQUIRED (static fetch returns only navigation); PDFs are ~95MB on
# Drive, so a token-handling downloader is required.
playwright>=1.40        # then run once: playwright install chromium
gdown>=5.0

# parse: QuizLink PDF -> (clean_I, q, y)
pypdf>=4.0              # PDF structure: AcroForm button widgets, /OCProperties
pymupdf>=1.24           # fitz: page images (Im0 jp2) + widget rects
opencv-python>=4.8      # HSV blue-leader segmentation + label-box inpainting
pytesseract>=0.3        # OCR of clean label boxes (needs system `tesseract`)
